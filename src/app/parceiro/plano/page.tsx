'use client';
import { useState } from 'react';
import { PortalShell } from '@/components/portal-shell';
const verified=['Reivindicar e corrigir perfil','Horários e condições de acesso','Disponibilidade de banheiro e água','Informações confirmadas pelo local'];
const partner=['Perfil enriquecido e fotos','Cardápio e promoções','QR para validar visitas','Resgates e métricas de conversão'];
export default function PlanPage(){const [interest,setInterest]=useState(false);return <PortalShell type="partner" title="Plano e assinatura"><div className="max-w-[900px] space-y-4"><h2 className="text-[26px] font-semibold">Verificado ou Parceiro</h2><p className="text-sm text-[#687471]">Dados corretos e acesso claro são o ponto de partida. Recursos comerciais são opcionais no piloto.</p><div className="grid gap-[18px] md:grid-cols-2"><Plan name="Verificado" price="R$ 0" note="Plano atual · controle operacional" items={verified} soft/><Plan name="Parceiro" price="Preço em teste" note="Aquisição local mensurável" items={partner}><button onClick={()=>setInterest(true)} className="h-[50px] w-full rounded-[14px] bg-[#0e5c57] text-sm text-white">{interest?'Interesse registrado':'Tornar-se Parceiro'}</button></Plan></div><section className="rounded-[14px] border border-[#e4e9e6] bg-[#eef4f1] p-4"><p className="text-[13px] font-medium text-[#0e5c57]">Preço ainda é hipótese do piloto: R$ 49 a R$ 99/mês.</p><p className="mt-1 text-xs text-[#687471]">Parceiros não passam à frente de opções claramente mais úteis numa busca urgente.</p></section></div></PortalShell>}
function Plan({name,price,note,items,soft,children}:{name:string;price:string;note:string;items:string[];soft?:boolean;children?:React.ReactNode}){return <section className={`rounded-[18px] border border-[#e4e9e6] p-[22px] ${soft?'bg-[#eef4f1]':'bg-white'}`}><h3 className="text-xl font-semibold">{name}</h3><p className="mt-2 text-[28px] font-semibold text-[#0e5c57]">{price}</p><p className="mt-2 text-[13px] text-[#687471]">{note}</p><div className="my-4 space-y-2">{items.map(x=><p key={x} className="rounded-xl border border-[#e4e9e6] bg-white p-3 text-[13px] font-medium">{x}</p>)}</div>{children}</section>}
